from elevenlabs.client import ElevenLabs

# ✅ Step 1: Initialize the client with your API key
client = ElevenLabs(
    api_key='sk_402e8e3e71a3963773172a8ad1567714f712f294b25e4fba',
)

# ✅ Step 2: Get all available models (test request)
models = client.models.get_all()

# ✅ Step 3: Print them
print("🎤 Available Models from ElevenLabs:")
for model in models:
    print(f"- {model.name} (ID: {model.model_id})")
